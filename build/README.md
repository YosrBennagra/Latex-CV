# LaTeX Build Directory

This directory contains auxiliary files generated during LaTeX compilation (.aux, .log, .fls, .fdb_latexmk, .out, .synctex.gz).

Keep this folder to avoid cluttering your source directories.
